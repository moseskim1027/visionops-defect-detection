apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: visionops
  labels:
    app: prometheus
spec:
  selector:
    app: prometheus
  ports:
    - name: http
      port: 9090
      targetPort: 9090
      protocol: TCP
  type: ClusterIP
