# Inference-only dependencies (no training stack)
# CPU-only torch — --extra-index-url must be a standalone line in requirements files
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchvision==0.21.0
ultralytics==8.3.40

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
Pillow==11.1.0
numpy==2.2.1
prometheus-client==0.21.1
mlflow==2.19.0
pyyaml==6.0.2
