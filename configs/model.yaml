mlflow:
  experiment_name: visionops-defect-detection
  model_name: visionops-yolov8n
  tracking_uri: http://localhost:5001
model:
  imgsz: 640
  task: detect
  variant: runs/detect/train3/weights/last.pt
promotion:
  map50_threshold: 0.1
training:
  batch: 8
  device: cpu
  epochs: 3
  lr0: 0.01
  patience: 5
  workers: 2
